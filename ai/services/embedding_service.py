class EmbeddingService:
    """
    Placeholder for embedding generation.

    In the next phase, this service will generate vector embeddings
    for document chunks and user quaries.
    """

    @staticmethod
    def generate_embedding(text: str):
        raise NotImplementedError(
            "Embedding generation will be implemented in Phase 15."
        )